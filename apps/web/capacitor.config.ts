import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "br.com.muriloneder.financplantoes",
  appName: "FinancPlantoes",
  webDir: "dist",
};

export default config;
